const CACHE='floralab-1.3.1-shell-refresh';
const CORE=['./','./index.html','./style.css','./runtime.js','./library.js','./app.js','./manifest.webmanifest','./data/catalog.json','./data/material-visuals.json','./data/material-visual-queries.json','./data/material-visual-sources.json','./icons/icon-32.png','./icons/icon-192.png','./icons/icon-512.png','./icons/icon-180.png','./assets/hero-render-study.webp'];
const NETWORK_FIRST=['/','/index.html','/style.css','/runtime.js','/library.js','/app.js','/manifest.webmanifest'];
function isNetworkFirst(request){try{const u=new URL(request.url);return NETWORK_FIRST.some(x=>x==='/'?u.pathname.endsWith('/'):u.pathname.endsWith(x));}catch{return false;}}
async function remember(request,response){if(response&&response.status===200&&response.type!=='opaque'){const c=await caches.open(CACHE);await c.put(request,response.clone());}return response;}
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(c=>c.addAll(CORE)).then(()=>self.skipWaiting()));});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()));});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;if(isNetworkFirst(event.request)){event.respondWith(fetch(event.request).then(res=>remember(event.request,res)).catch(()=>caches.match(event.request).then(hit=>hit||caches.match('./index.html'))));return;}event.respondWith(caches.match(event.request).then(hit=>hit||fetch(event.request).then(res=>remember(event.request,res)).catch(()=>caches.match('./index.html'))));});
